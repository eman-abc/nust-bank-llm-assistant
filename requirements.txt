# =============================================================================
# NUST Bank LLM Assistant — single environment for Gradio app + notebooks
# =============================================================================
# Install: pip install -r requirements.txt
# GPU: sentence-transformers pulls CPU torch from PyPI by default. For CUDA,
#       install the matching torch build from https://pytorch.org first, then
#       run pip install -r requirements.txt (use --no-deps on ST if needed).

# --- Gradio UI + RAG runtime (frontend/gradio_app.py) ---
gradio>=4.44.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
huggingface-hub>=0.24.0
python-dotenv>=1.0.0
langchain-text-splitters>=0.3.0
numpy>=1.26.0
pandas>=2.0.0

# --- Notebooks: Excel ingestion (notebooks/01_data_ingestion.ipynb) ---
openpyxl>=3.1.0

# --- Notebooks: progress UI (notebooks/02_indexing.ipynb) ---
tqdm>=4.66.0
ipywidgets>=8.0.0

# --- Optional: run Jupyter locally ---
jupyter>=1.0.0
ipykernel>=6.29.0

# --- Backend LangGraph (orchestrator + nodes) ---
langgraph>=0.2.0

# --- Backend security (evaluator) ---
# After install, download the spaCy model: python -m spacy download en_core_web_lg
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0
